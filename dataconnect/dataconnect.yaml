specVersion: "v1"
serviceId: "note-manager"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "note-manager-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
